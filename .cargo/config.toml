# Required for `getrandom` 0.3 on wasm32 — see md500-wasm/Cargo.toml.
# Without this flag, any transitive `getrandom` user fails to compile for
# wasm32-unknown-unknown.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
